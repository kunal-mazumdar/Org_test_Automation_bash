pipelines:
  - name: S_Bash_10983_0001
    steps:
      - name: S_Bash_10983_0001
        type: Bash
        configuration:
          nodePool: S_BASH_GENERIC_k8sdynamic
        execution:
          onExecute:
            - mkdir -p test && mount /dev/sdb test
