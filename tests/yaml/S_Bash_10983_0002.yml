pipelines:
  - name: S_Bash_10983_0002
    steps:
      - name: S_Bash_10983_0002
        type: Bash
        configuration:
          nodePool: S_BASH_GENERIC_k8sdynamic
        execution:
          onExecute:
            - docker run --rm --privileged alpine ls
